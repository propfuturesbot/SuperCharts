# React Development Server Configuration
# Allow access from Cloudflare tunnel hosts

# Disable host check to allow Cloudflare tunnel access
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Allow all hosts (required for Cloudflare tunnel)
WDS_SOCKET_HOST=0.0.0.0